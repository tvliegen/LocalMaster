# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
# development:
#  adapter: sqlite3
#  database: db/development.sqlite3
#  pool: 5
#  timeout: 5000

development:
  adapter: postgresql
  encoding: unicode
  database: teknion
  pool: 5
  username: <%= ENV['POSTGRES_USER'] ? ENV['POSTGRES_USER'] : "apps" %>
  password: <%= ENV['POSTGRES_PASS'] ? ENV['POSTGRES_PASS'] : "teknion" %>

